# 開発進捗ログ（最終更新: 2025-12-22 13:49:54）

## 今回までに完了した内容

- 市場データ取得を `MarketDataProvider` 抽象に置き換え、キャッシュ付きシンセティックデータへのフェイルオーバーを実装。
- `/api/optimization` を非同期化し、ポートフォリオ正規化後にメタ情報（データソース/キャッシュ/レンジ）を返すようバリデーション強化。
- ダッシュボードの最適化セクションにローディング/エラーの `aria-live` ステータスとリトライUIを追加し、ソース判別メッセージを表示。
- 最適化サービス向けのユニットテストを追加し、キャッシュ挙動とフェイルオーバーを検証。
- `/manual` のユーザーマニュアルを追加し、ダッシュボード導線と高度分析の見方を整備。
- 日本語/英語の切り替えをダッシュボード全体へ反映し、ロケールを `localStorage` に保持。
- シミュレーション設定の対象資産を配分計算・チャート表示へ反映。

## 次回以降の主なTODO

- Yahoo Finance など実データプロバイダ実装を追加し、環境変数経由でキーを注入した上で `MarketDataProvider` を差し替える。
- UIでデータソース（外部/キャッシュ/シミュレーション）を明示し、チャート・表にローディングスケルトンを導入する。
- E2E/ユニットテストを拡充し、外部APIモックとキャッシュ分岐をカバーする。
